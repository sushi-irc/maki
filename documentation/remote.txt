                                     REMOTE

To use maki remotely, use the following instructions.

On the server:
  $ maki --dbus-server

On the client (this requires that you can log in as $user on $host via SSH):
  $ maki-remote $user@$host tekka

# vi:textwidth=80:expandtab
